export let bottomToTop = document.getElementById(
  "bottom"
) as HTMLInputElement | null;

export let stickyNavbar = document.getElementById(
  "navbar"
) as HTMLInputElement | null;

export let modal = document.getElementById(
  "showModal"
) as HTMLInputElement | null;

export let showScrollBar = document.getElementById(
  "scrollBar"
) as HTMLInputElement | null;
